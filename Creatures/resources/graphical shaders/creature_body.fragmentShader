#version 460 core

in vec4 oVertexColor;
in float oVertexDepth;

out vec4 FragColor;
out float gl_FragDepth;

void main()
{
    FragColor = oVertexColor * pow(1.0 - oVertexDepth, 0.25);
    gl_FragDepth = oVertexDepth;
}
