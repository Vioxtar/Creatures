#version 460 core

layout (std430, binding = 0) buffer CreatureDeformerCounts
{
	uint DeformerCounts[];
};


layout (local_size_x = @LOCAL_SIZE@, local_size_y = 1, local_size_z = 1) in;

void main()
{
	uint gid = gl_GlobalInvocationID.x;
	DeformerCounts[gid] = 0;
}